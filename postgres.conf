# Generated by PGConfig 3.1.2 (5c1bbed5df8b2e1a63ef02bd759ef59016a923cc)
# https://api.pgconfig.org/v1/tuning/get-config?format=conf&&log_format=csvlog&max_connections=100&pg_version=16&environment_name=Mixed&total_ram=1GB&cpus=3&drive_type=SSD&arch=x86-64&os_type=linux

# Memory Configuration
shared_buffers = 128MB
effective_cache_size = 384MB
work_mem = 1MB
maintenance_work_mem = 26MB

# Checkpoint Related Configuration
min_wal_size = 2GB
max_wal_size = 3GB
checkpoint_completion_target = 0.9
wal_buffers = -1

# Network Related Configuration
listen_addresses = '*'
max_connections = 100

# Storage Configuration
random_page_cost = 1.1
effective_io_concurrency = 200

# Worker Processes Configuration
max_worker_processes = 8
max_parallel_workers_per_gather = 2
max_parallel_workers = 2

# Logging
#log_min_messages=PANIC

# Adjust the minimum time to collect the data
log_min_duration_statement = 0
log_autovacuum_min_duration = 0

# Debug Logs
log_statement = mod
log_duration = 1